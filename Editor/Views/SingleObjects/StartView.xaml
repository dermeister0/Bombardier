<UserControl x:Class="Bombardier.Editor.Views.SingleObjects.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Canvas.Left="{Binding LocalX}" Canvas.Top="{Binding LocalY}"
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid>
        <Rectangle Width="50" Height="50" Stroke="YellowGreen" StrokeThickness="5" />
        <TextBlock TextAlignment="Center" VerticalAlignment="Center" Foreground="GreenYellow">START</TextBlock>
    </Grid>
</UserControl>
