<Window x:Class="Bombardier.Editor.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cc="clr-namespace:Bombardier.Editor"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="File">
                <MenuItem Header="New..." Command="{Binding FileNewCommand}" />
                <MenuItem Header="Load..." />
                <MenuItem Header="Save..." />
            </MenuItem>
        </Menu>
        <ScrollViewer Name="MainScrollViewer" HorizontalScrollBarVisibility="Auto" Grid.Row="1">
            <cc:MapBody/>
        </ScrollViewer>
    </Grid>
</Window>
